import React from 'react';
// import { useLoaderData } from 'react-router-dom';
// import { ResponsiveContainer, LineChart, Line } from 'recharts';

const Statistics = () => {
    // const dataObj = useLoaderData();
    // const datas = dataObj.data;
    // console.log(datas)
    // const data = []
    return (
       
        <div>
             {/* {
            datas.map(quiz => console.log(data.push(quiz.total)))
            }
           <ResponsiveContainer width="100%" height="100%">
                <LineChart width={500} height={100} data={data}>
                <Line type="monotone" dataKey="data.total" stroke="#82ca9d" strokeWidth={2} />
                </LineChart>
            </ResponsiveContainer> */}
        </div>
    );
};

export default Statistics;